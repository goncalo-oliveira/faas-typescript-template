language: typescript-fastify
fprocess: node index.js
welcome_message: |
  You have created a new function which uses Node.js 14 (LTS), Fastify, TypeScript
  and the OpenFaaS of-watchdog which gives greater control over HTTP responses.
